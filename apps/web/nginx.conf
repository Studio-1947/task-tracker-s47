server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Long-cache hashed assets.
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }

  # Socket.IO (chat realtime). Served at /socket.io on the API (NOT under /api),
  # so it needs its own location with WebSocket upgrade headers.
  location /socket.io/ {
    proxy_pass http://api:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Cookie $http_cookie;
    # Long-lived connections; don't cut idle sockets prematurely.
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
  }

  # Reverse-proxy API calls to the api service on the internal network.
  location /api/ {
    # File uploads (attachments up to 20 MB) — nginx default is 1m.
    client_max_body_size 25m;
    proxy_pass http://api:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Cookie $http_cookie;
  }

  # SPA fallback.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
